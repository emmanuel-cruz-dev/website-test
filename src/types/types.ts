export interface ProductCardProps {
  name: string;
  description: string;
  weight: string;
  brand: string;
  price: number;
  stock: number;
  image: string;
  category: string;
  promotion: boolean;
}
